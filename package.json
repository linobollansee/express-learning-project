{
  "name": "express-learning-project", // The name of your project
  "version": "1.0.0",                 // Project version
  "description": "A learning-focused Express.js project demonstrating routing, middleware, templating, API, validation, and tests.", // Short project description
  "main": "src/server.js",            // Entry point of the application
  "scripts": {
    "start": "node src/server.js",    // Command to start the server in production
    "dev": "nodemon --watch src --exec \"node src/server.js\"", // Command to start server with live reload for development
    "test": "jest --watchAll=false --runInBand" // Run tests once with Jest
  },
  "author": "Generated by ChatGPT",   // Author of the project
  "license": "MIT",                   // License type
  "dependencies": {
    "cors": "^2.8.5",                 // Middleware for enabling CORS
    "ejs": "^3.1.10",                 // Template engine for rendering HTML
    "express": "^4.18.2",             // Web framework for Node.js
    "helmet": "^6.0.1",               // Security middleware to set HTTP headers
    "morgan": "^1.10.0",              // HTTP request logger middleware
    "dotenv": "^16.0.3",              // Loads environment variables from .env file
    "express-validator": "^7.0.1"     // Middleware for validating and sanitizing input
  },
  "devDependencies": {
    "jest": "^29.6.1",                // Testing framework
    "supertest": "^6.4.2",            // Library for testing HTTP endpoints
    "nodemon": "^2.0.22"              // Automatically restarts server during development
  }
}
